{
  "_note": "Slide 20, right-hand side. Two differences from the slide: custom_rules take the rule IDs returned by POST _plugins/_security_analytics/rules (bin/detect.py substitutes __RULE_ID_1__ / __RULE_ID_2__), and the trigger needs an 'ids' list naming the rules it fires on. The pre-packaged aws_console_login_no_mfa rule on the slide belongs to the cloudtrail log type, not others_application - a detector carries exactly one detector_type, so that rule lives in its own CloudTrail detector rather than this one.",
  "name": "agent-harness",
  "type": "detector",
  "detector_type": "others_application",
  "enabled": true,
  "schedule": { "period": { "interval": 1, "unit": "MINUTES" } },
  "inputs": [
    {
      "detector_input": {
        "description": "Agent harness policy violations in agent trace spans",
        "indices": ["__INDEX__"],
        "pre_packaged_rules": [],
        "custom_rules": [{ "id": "__RULE_ID_1__" }, { "id": "__RULE_ID_2__" }]
      }
    }
  ],
  "triggers": [
    {
      "name": "high-and-above",
      "severity": "1",
      "ids": ["__RULE_ID_1__", "__RULE_ID_2__"],
      "types": [],
      "tags": [],
      "sev_levels": ["high", "critical"],
      "actions": []
    }
  ]
}
